#include<stdio.h>
#include<math.h>
int main()
{
  long n;
  int count = 0;
  do
  {
    printf("\nNhập vào số nguyên n lớn hơn 0: ");
    scanf("%ld", &n);
    if(n <= 0)
    {
      printf("\n Số n phải lớn hơn 0, vui lòng nhập lại !");
    }
  }while(n <= 0);
  while(n!=0){
   n = n / 10 ;
   count++;
  }
  printf("So luong chu so cua so nguyen duong n la: %d",count);
}
