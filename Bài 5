#include<stdio.h>
int main()
{
  int n;
  float S;
  S = 0;
  printf("\nNhập vào số n: ");
    scanf("%d", &n);
  for(int i = 0; i <= n; i++){
    S = S + 1.0 / ((2 * i) + 1);
  }
   printf("\nTổng 1 + 1/3 + ... + 1/%d là: %.2f",((n * 2) + 1), S);
}
