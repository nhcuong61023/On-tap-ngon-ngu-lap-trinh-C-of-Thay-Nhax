#include <stdio.h>

int main() {
  int n, sum = 0;
  printf("\nNhập vào số nguyên dương n: ");
  scanf("%d", &n);
  for(int i = 1; i < n; i +=2){
       sum = sum + i;
  }
    printf("Tong cac so le tu 1 den n la: %d",sum);
}
