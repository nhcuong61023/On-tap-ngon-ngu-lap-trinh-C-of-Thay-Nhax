#include<stdio.h>
int main()
{
  int n;
  float S;
  S = 0;
  printf("\nNhập vào số n: ");
    scanf("%d", &n);
   for(int i = 1; i <= n; i++){
    S = S + 1.0 / (2 * i);
  }
   printf("\nTổng 1/2 + 1/4 + ... + 1/%d la: %.2f",n * 2, S);
}
