#include<stdio.h>
int main()
{
  int n;
  float S;
  S = 0;
  printf("\nNhập vào số n: ");
    scanf("%d", &n);
   for(int i = 1; i <= n; i++){
    S = S + 1.0 / i;
  }
  printf("\nTổng 1 + 1/2 + ... + 1/%d là: %.2f",n, S);
}
