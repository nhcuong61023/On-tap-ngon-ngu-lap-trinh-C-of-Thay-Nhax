#include <stdio.h>

int main() {
    int A[100], i, n, flag = 1;
    printf("\n Nhap n=");
    scanf("%d", &n);
    printf("\n Nhap mang: \n");
    for (i = 0; i < n; i++) {
        printf("A[%d]=", i);
        scanf("%d", &A[i]);
    }
    for (i = 0; i < n - 1; i++) {
        if ((A[i] <= A[i + 1] && A[i + 1] >= A[i + 2]) ||(A[i] >= A[i + 1] && A[i + 1] <= A[i + 2])) {
            flag = 3;
            printf("day ko tang cung ko giam");
            break;
        } else{
            if (A[i + 1] < A[i]) {
                flag = 0;
                printf("\n Day la day giam dan \n");
                break;
            }
        }
    }
    if (flag == 1) {
        printf("\n Day la day tang dan \n");
    }
}
